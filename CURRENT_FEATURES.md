# Tagger 当前功能说明

> 本文对应当前仓库代码，用于在继续开发、测试和发布前确认功能边界。

## 1. 项目定位

Tagger 是一个本地优先的轻量 CSV/TSV 数据标注器：

- 用普通表格的方式查看和编辑数据。
- 用单元格、范围、整行、整列或整张表快速添加标签。
- 标签带有名称、定义、颜色和快捷键。
- 表格数据与标签数据分开保存，便于普通工具和大模型分别读取。

当前实现是 React + Vite 前端，并包含 Tauri 桌面应用配置。浏览器版和桌面版都加载同一份 `src/` 前端代码；差别主要在运行容器和文件大小适用范围。

## 2. 当前已实现功能

### 导入与表格查看

- 支持 `.csv` 和 `.tsv`。
- 根据文件扩展名自动使用逗号或 Tab 分隔符。
- 支持带引号的字段、字段内分隔符、换行和双引号转义。
- 首行作为表头，其余内容作为数据行。
- 表格区域支持横向、纵向滚动。
- 数据行采用可视区域渲染，减少大行数表格一次性渲染造成的卡顿。
- 文件解析放在 Web Worker 中，不阻塞主要界面。
- 单元格选中后，顶部会显示完整内容预览。
- 表头列宽可以拖动调整，最小宽度为 36px。

### 两种工作模式

默认进入“标注模式”，使用 `Shift+L` 切换：

| 模式 | 表格内容 | 标签栏 | 主要操作 |
| --- | --- | --- | --- |
| 标注模式 | 锁定 | 显示 | 快捷键或点击标签，对选区添加/移除标签 |
| 编辑模式 | 可编辑 | 隐藏 | 修改、清空、复制和粘贴表格内容 |

标注模式下，`Delete` 或 `Backspace` 删除选区中的标签，不删除原始文本。

编辑模式下，`Delete` 或 `Backspace` 清空选区中的表格文本；双击单元格或按 `Enter` 可以编辑内容。

### 选择范围

- 单击或拖动选择单元格范围。
- 单击行号选择整行。
- 单击表头选择整列。
- `Shift` 扩展连续行或列选择。
- `Cmd/Ctrl` 追加或取消选择行、列。
- `Shift + 方向键` 扩展单元格范围。
- 方向键移动当前选择。
- `Tab` 在单元格之间移动，`Shift+Tab` 反向移动。
- `Cmd/Ctrl+A` 选择整张表。
- `Escape` 清除选择并关闭当前浮层。

### 标签

每个标签包含：

- 名称
- 定义
- 填充颜色
- 组合快捷键

标签支持：

- 新建、编辑和删除。
- 预设颜色点选择。
- 系统颜色选择器选择自定义颜色。
- 点击快捷键区域后，直接按下一个键或组合键完成录入。
- 支持 `Cmd/Ctrl/Alt/Shift` 组合键，例如 `Cmd+1`。
- 点击标签行，将当前标签应用到当前选区。
- 标注模式下按已设置的快捷键添加标签。
- 只按 `Shift+快捷键` 时移除标签。
- 删除标签时，同时清理所有关联标注。

标签作用范围为当前选择的单元格、范围、行、列或整张表。一个单元格可以有多个标签，行、列和整表标签也会参与该单元格的有效标签计算。

### 标签显示方式

标签栏内可以切换两种显示方式：

- **填充**：用第一个标签的颜色突出显示单元格；一个单元格有多个标签时，额外显示颜色概览。
- **圆点**：为每个标签显示一个颜色点，适合同时查看多个标签。

标签栏支持：

- 拖动标题栏变为浮动面板。
- 向左、右、上、下边缘拖动并吸附。
- 吸附时出现蓝色提示线。
- 拖动边界调整吸附面板宽度或高度。
- 浮动时拖动右下角调整大小。

### 筛选

顶部搜索框可以搜索所有字段的文本，支持 `Cmd/Ctrl+K` 和 `Cmd/Ctrl+F` 聚焦。

每个表头右侧的下拉按钮会打开该列的筛选面板。面板有两个选项：

#### 按值筛选

- 输入文字搜索该列的值。
- 显示去重后的值列表和出现次数。
- 勾选或取消勾选指定值。
- `全选`、`全不选`、`反转勾选`。
- 支持“正选”：只显示勾选值。
- 支持“反选”：隐藏勾选值，显示其他值。

#### 按标签筛选

- 选择一个标签。
- 匹配该列单元格的有效标签，也会匹配相关行标签。

多个列的值筛选、文本筛选和标签筛选可以同时使用，条件之间按“并且”叠加。筛选面板可以通过点击其他区域、按 `Enter` 或按 `Escape` 关闭；“清除筛选”会恢复全部行。

### 显示列

顶部“列”菜单用于控制参考列：

- 单独隐藏或显示列。
- `反选`当前显示状态。
- `全部显示`恢复所有列。

隐藏列只影响当前查看，不会删除 CSV 内容或标签。

### 保存、撤销与存档

- `Cmd/Ctrl+S` 保存当前修改。
- `Cmd/Ctrl+Z` 撤销。
- `Cmd/Ctrl+Shift+Z` 或 `Cmd/Ctrl+Y` 重做。
- `Cmd/Ctrl+O` 返回存档/导入界面。
- 离开有未保存修改的表格时，会询问保存、不保存或取消。
- 当前工作区会自动同步到所占用的存档槽位。

存档界面提供 10 个位置：

- 导入单个或多个 CSV/TSV，分别占用存档位。
- 支持拖入导入。
- “新建标注”只导入表格并创建空标签状态。
- “恢复 CSV + 标签”用于同时导入表格和标签文件。
- 支持导入 Tagger 导出的 `.tagger.zip`。
- 可勾选多个存档批量导出。
- 删除存档前需要二次确认。
- 从工作区返回不会自动删除存档。

## 3. 文件与标签格式

### 单独文件

Tagger 不把颜色和标签写入 CSV/TSV 文本本身，而是使用同名标签文件：

```text
records.csv
records.tags.json
```

桌面端也兼容读取旧版本生成的 `records.csv.tags.json`，新保存统一使用 `records.tags.json`。

标签 JSON 使用 `tagger.tags/v1` 格式，主要包含：

```json
{
  "schema": "tagger.tags/v1",
  "version": 1,
  "source": {
    "file": "records.csv",
    "delimiter": "comma"
  },
  "tags": {},
  "annotations": {
    "rows": {},
    "cells": {},
    "columns": {},
    "dataset": []
  }
}
```

标注定位规则：

- 行：`row-0`、`row-1`，从 0 开始计数。
- 单元格：`row-0` 加表头名称。
- 列：使用表头名称。
- 整表：使用 `dataset`。

因此，单独打开 CSV 时只能看到表格内容；要恢复颜色、标签定义和标注位置，必须同时导入 CSV/TSV 与对应的标签 JSON。

### `.tagger.zip`

选择“CSV + 标签”导出时，Tagger 会生成配套 ZIP。单个存档通常包含：

```text
01-records/
  records.csv
  records.tags.json
  tagger-tags-template.json
```

多存档批量导出时，每个存档会放入独立目录。该 ZIP 可以作为一个整体重新导入并恢复标签，也可以解压后分别查看 CSV 和 JSON。

ZIP 中的 CSV 是普通 CSV/TSV，标签信息在 JSON 中。大模型可以读取 CSV 的行列内容，再依据 JSON 中的 `tag_definitions`/标签定义和 `annotations` 还原哪些行、列或单元格被标注以及标签含义；标签不会凭空出现在 CSV 单文件中。

当前 ZIP 使用 Tagger 自己实现的未压缩 ZIP 写入和读取，恢复入口主要面向 Tagger 导出的 ZIP。

### 标签导入与样例

在标签栏可以导入 `.json` 标签文件。导入器兼容：

- Tagger 完整标签导出文件。
- 带 `tags` 数组的标签文件。
- 以标签名为键、定义为值或对象的简化 JSON。

标签栏下方提供标签样例 JSON，可复制给大模型设计标签，再将生成的 JSON 导入 Tagger。标签文件可以只包含标签定义，也可以同时包含已有标注。

## 4. 导出选项

工作区右上角“导出”菜单提供：

- **CSV + 标签**：默认，生成 `.tagger.zip`。
- **只导出 CSV**：生成普通 `.csv` 或 `.tsv`。
- **只导出标签**：生成 `.tags.json`。

当前工作区导出 CSV 或 CSV + 标签后，会询问是否清理当前存档；只导出标签不会清理存档。

存档界面右上角也可以对勾选的存档执行相同选择。批量选择“CSV + 标签”会生成一个包含多个存档目录的 ZIP。

## 5. 启动与构建

### 环境要求

- Node.js 18 或更高版本。
- npm。
- 构建桌面版还需要 Rust 和 Cargo。

### 浏览器开发版

```bash
npm ci
npm run dev
```

打开终端显示的本地地址即可使用。浏览器版当前只建议导入小于 3 MB 的文件，达到或超过限制时会提示使用桌面安装版。

### Tauri 桌面开发版

```bash
npm run tauri dev
```

桌面应用使用 Tauri 窗口运行，和浏览器版共用 `src/` 前端。开发服务器固定使用 `5173` 端口；如果端口被占用，Vite 会直接报错，避免桌面端误加载其他项目的页面。

### 构建安装包

```bash
npm run tauri build
```

构建结果位于：

```text
src-tauri/target/release/bundle/
```

具体安装包格式取决于构建系统和目标平台，例如 macOS 的 `.dmg`、Windows 的安装包和 Linux 的安装包。发布时应在对应操作系统上分别构建和测试。

仓库也提供：

```bash
./dev.sh
./build.sh
```

## 6. 当前架构

```text
src/
  App.tsx                 页面、状态和交互逻辑
  App.css                 界面样式
  csvParser.ts            CSV/TSV 解析和文件流读取
  csvParser.worker.ts     Worker 解析入口
  types/index.ts          数据、标签和标注类型
  main.tsx                React 入口
src-tauri/
  src/main.rs             Rust 文件读写、标签保存和桌面命令
  tauri.conf.json         桌面窗口和打包配置
public/
  tagger-icon.svg         浏览器、页面和 README 使用的图标
```

## 7. 当前边界与待修正项

以下内容不应在当前版本文档中被表述为已经完成：

- 浏览器端不是 200 MB 级别的稳定方案；当前浏览器会拦截 3 MB 及以上文件。
- 虽然表格使用 Worker 解析和可视行渲染，但解析结果仍会整体保存在内存中，超大文件仍可能受内存影响。
- CSV 本体不携带标签，跨工具传递时必须保留 `.tags.json` 或 `.tagger.zip`。
- 当前 ZIP 是未压缩格式，主要用于结构化打包和恢复，不以压缩体积为目标。
- 当前列筛选支持文本包含、值列表正选/反选和单标签筛选，但还不是完整的 WPS/Excel 功能集。
- 当前表格编辑提供单元格编辑、清空、复制、粘贴和撤销重做，不包含公式、排序、冻结窗格、插入/删除行列等高级表格能力。
- 发布前仍应在目标系统实测大文件打开、标签恢复、CSV/TSV 编码、快捷键和 Tauri 安装包。

## 8. 提交前检查

```bash
npm ci
npm run build
cargo check --manifest-path src-tauri/Cargo.toml
git status
```

不要提交以下生成内容：

```text
node_modules/
dist/
src-tauri/target/
src-tauri/gen/
*.tagger.zip
```
